<form method="post" action="{{ path('images_random_delete', {'id': images_random.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ images_random.id) }}">
    <button class="btn">Delete</button>
</form>
