<form method="post" action="{{ path('images_random_delete', {'id': kitchen.id}) }}" onsubmit="return confirm('Supprimer l\'image ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kitchen.id) }}">
    <button class="btn">Supprimer</button>
</form>